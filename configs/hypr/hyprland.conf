monitor=,preferred,auto,1

input {
    kb_layout = us
}

general {
    gaps_in = 5
    gaps_out = 10
    border_size = 2
}

decoration {
    rounding = 10
}

exec-once = /usr/bin/kitty

bind = SUPER, Return, exec, /usr/bin/kitty
bind = SUPER, Q, killactive
bind = SUPER, M, exit
