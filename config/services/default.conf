# Arch Workstation service manifest
#
# Format:
# service-name|policy
#
# Policies:
# required  Enabled and started during normal bootstrap
# optional  Enabled only when explicitly requested

NetworkManager|required
sshd|optional
